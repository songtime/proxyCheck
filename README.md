# proxyCheck

用于解析订阅节点并做可用性检测的脚本集合。

**macOS 额外工具**
以下工具在 macOS 上需要自行安装（脚本会用到）：
- `ss-local`（来自 `shadowsocks-libev` 或 `shadowsocks-rust`）
- `jq`
- `python3`

macOS 自带无需安装：
- `curl`
- `nc`
- `base64`
- `awk`
